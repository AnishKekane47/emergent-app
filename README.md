# Emergent App

This is a fraud detection application.

## Frontend

The frontend is a React application.

To start the frontend, run:

```bash
cd frontend
npm install
npm start
```

## Backend

The backend is a Python Flask application.

To start the backend, run:

```bash
cd backend
pip install -r requirements.txt
python server.py
```
